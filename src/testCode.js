/*
  storeFunctionality() {
    for (let i = 0; i < 7; i++) {
      switch (i) {
        case 0:
          if (fishCount >= this.fishPole.price) {
            fishCount -= this.fishPole.price;
            this.fishPole.ammount += 1;
            this.fishpole.price *= 1.2;
            console.log(this.fishpole.ammount);
          }
          break;
        case "netBtn":
        case "boatBtn":
        case "crewBtn":
        case "yachtBtn":
        case "fishFactoryBtn":
        case "fishPortalBtn":
        default:
          break;
      }
    }
  } */
/*
  store(fishCount) {
    for (let i = 0; i < this.storeBtns.length; i++) {
      this.storeBtns.[i].addEventListener("click", function() {
        let btnId = this.storeBtns[i].id;

        switch (btnId) {
          case "fishPoleBtn":
            if (fishCount >= this.fishPole.price) {
              fishCount -= this.fishPole.price;
              this.fishPole.ammount += 1;
              this.fishpole.price *= 1.2;
              console.log(this.fishpole.ammount);
            }
            break;
          case "netBtn":
          case "boatBtn":
          case "crewBtn":
          case "yachtBtn":
          case "fishFactoryBtn":
          case "fishPortalBtn":
          default:
            break;
        }
      });
    }
  } */

  /*
  function store(){
    for(i = 0; i < 7; i++){
      buyBtn[i].addEventListener('click', () => {
        switch(buyBtn[i].id){
          case "fishingPole":
            break;
          case "fishingPole":
            break;
          case "fishingPole":
            break;
          case "fishingPole":
            break;
          case "fishingPole":
            break;
          case "fishingPole":
            break;
          case :
            break;

        }
      });
    }
  }
  */
